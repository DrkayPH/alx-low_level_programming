#include <stdio.h>

/**
 *main - this will serve as entry point
 *Return: this will be zero indicating success
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
